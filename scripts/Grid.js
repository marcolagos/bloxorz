export default class Grid {

      level;
      height;
      width;
      depth;

      constructor(level, height, width, depth) {
            
            this.level = level;
            this.height = height;
            this.width = width;
            this.depth= depth;

      }
      // grid will have a size amd ocnstructor that will initlizate the pivt points
      // the pivots points arent gpoing to be everywhere
      // the constructor can have a helper function that will initialize the pivot points
      // orgin, which will start the first plate, the pivot orgin is slightly shifted
}